/** Horizontal placement of an icon relative to adjacent text or content. */
export type IconPosition = "start" | "end";

/**
 * Default width/height for inline icons beside labels (buttons, inputs, dropdown rows, badges).
 * Responsive: slightly larger tap/viewport on small screens, compact from `md` up.
 */
export const INLINE_ICON_WITHIN_CLICKABLE_CLASS = "size-[18px] md:size-4";
